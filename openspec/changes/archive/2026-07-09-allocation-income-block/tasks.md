## 1. Core UI Implementation

- [ ] 1.1 Implement the Total Income to Allocate Block requirement inside `DashboardTab.tsx` as per Decision 1: Render Top Allocation Income Block to display total bound monthly income at the top of the account groups section. Verification: Confirm the card displays correctly on the dashboard with the correct green briefcase icon and total amount.
- [ ] 1.2 Implement the Account Group Target and Reached Dollar Amount Display requirement inside `DashboardTab.tsx` to render target and actual dollar amounts on card subtexts based on Decision 2: Display Target and Actual Dollar Ratios on Subtexts. Verification: Verify that the card subtexts update with the correct math values (e.g. `目標 30% ($15,000)`) in the browser.
